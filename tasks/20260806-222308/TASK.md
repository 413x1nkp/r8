# Add a small example of a polyglot ROM

- STATUS: OPEN
- PRIORITY: 100
- TAGS: stream

It's a ROM that acts both as a ROM and something else, for example a
.png image. The idea is to make ROMs PICO-8 style. People have already
done that in the wild. So we need maybe an example to put into the
[examples/](../../examples/) folder.
